#include "game_process.hpp"

#include "tmap_parser.hpp"
#include "graphics/render.hpp"

// Load a map file, returns true on success
bool setTmap(const std::string& filePath) {
    // TMapParser parser;
    // if (parser.loadFromFile(filePath)) {
    if (true) {
        return true;
    } else {
        return false;
    }
}

// Main game process function, run when game world is loaded
void runGameProcess() {
    // Implementation of the game process
    // This function would contain the main game loop and initialization logic
}